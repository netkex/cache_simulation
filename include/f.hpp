#include "Array.hpp"
#include <cstddef>

#ifndef CACHE_SIM_F_HPP
#define CACHE_SIM_F_HPP

void f(Array<double>& x,
       Array<double>& y,
       Array<double>& z,
       Array<double>& xx,
       Array<double>& yy,
       Array<double>& zz,
       std::size_t size,
       double w);

#endif //CACHE_SIM_F_HPP
